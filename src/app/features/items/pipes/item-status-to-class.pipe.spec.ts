import { ItemStatusToClassPipe } from './item-status-to-class.pipe';

describe('ItemStatusToClassPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemStatusToClassPipe();
    expect(pipe).toBeTruthy();
  });
});
