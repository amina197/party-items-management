import { CalculateProgressPipe } from './calculate-progress.pipe';

describe('CalculateProgressPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateProgressPipe();
    expect(pipe).toBeTruthy();
  });
});
